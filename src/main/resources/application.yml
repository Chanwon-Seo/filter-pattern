server:
  port: 8080

spring:
  application:
    name: filter-pattern
  datasource:
    url: jdbc:mysql://localhost:3307/product
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true